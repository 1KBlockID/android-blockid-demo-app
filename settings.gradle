dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://nexus-1k-nonprod.1kosmos.net/repository/maven-releases/'
            credentials {
                username = System.getenv("NEXUS_USERNAME") ?: "developer"
                password = System.getenv("NEXUS_PASSWORD") ?: "q5k#06ZcjSo#"
            }
        }
        maven { url "https://jitpack.io" }
    }
}

include ':app'
rootProject.name = "BlockIDSdkDemo"